# Overview
This writeup details the reverse engineering of an Android application that requires a `TODO`. The goal is to reverse engineer `TODO`.

## Initial analysis
First, the `AndroidManifest.xml` is examined to understand the app structure. Analysis reveals `TODO`.


## Java analysis
Next, the APK is decompiled with `jadx-gui` to examine `MainActivity.java`. `TODO`.
 

## Native library analysis
`Ghidra` is used to disassemble and decompile the `TODO` library. The main JNI function is `TODO`.

## Flag
`TODO`